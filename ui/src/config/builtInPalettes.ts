import { createPalette } from './paletteUtils';
import { darkPalette, defaultPalette } from './basePalettes';

export const palettes = {
  default: defaultPalette,
  dark: darkPalette,
  atlas: createPalette({
    bg: '#f6f8fb',
    surface: '#ffffff',
    ink: '#101828',
    accent: '#2557a7',
    success: '#16805d',
    danger: '#c03744',
    warning: '#b76e00',
  }),
  graphite: createPalette({
    bg: '#f4f6f8',
    surface: '#ffffff',
    ink: '#111827',
    accent: '#475467',
    success: '#027a48',
    danger: '#b42318',
    warning: '#b54708',
  }),
  nord: createPalette({
    bg: '#f5f7fa',
    surface: '#ffffff',
    ink: '#243447',
    accent: '#3b6ea8',
    success: '#2f855a',
    danger: '#c53030',
    warning: '#b7791f',
  }),
  cobalt: createPalette({
    bg: '#f7f9ff',
    surface: '#ffffff',
    ink: '#101828',
    accent: '#1d4ed8',
    success: '#047857',
    danger: '#be123c',
    warning: '#b45309',
  }),
  harbor: createPalette({
    bg: '#f4fbfb',
    surface: '#ffffff',
    ink: '#102a43',
    accent: '#0f766e',
    success: '#15803d',
    danger: '#b91c1c',
    warning: '#a16207',
  }),
  executive: createPalette({
    bg: '#f5f7fb',
    surface: '#ffffff',
    ink: '#111827',
    accent: '#334155',
    success: '#047857',
    danger: '#b91c1c',
    warning: '#92400e',
  }),
  iris: createPalette({
    bg: '#faf8ff',
    surface: '#ffffff',
    ink: '#1f1f39',
    accent: '#5b5bd6',
    success: '#087f5b',
    danger: '#c2255c',
    warning: '#b7791f',
  }),
  sage: createPalette({
    bg: '#f7faf7',
    surface: '#ffffff',
    ink: '#1f2933',
    accent: '#4d7c0f',
    success: '#15803d',
    danger: '#b91c1c',
    warning: '#a16207',
  }),
  copper: createPalette({
    bg: '#fbf8f4',
    surface: '#ffffff',
    ink: '#27211f',
    accent: '#9a5b13',
    success: '#2f855a',
    danger: '#a61b1b',
    warning: '#a16207',
  }),
  glacier: createPalette({
    bg: '#f5fbff',
    surface: '#ffffff',
    ink: '#172033',
    accent: '#0369a1',
    success: '#0f766e',
    danger: '#be123c',
    warning: '#b45309',
  }),
} as const;

export const paletteLabels: Record<keyof typeof palettes, string> = {
  default: 'Default',
  dark: 'Dark',
  atlas: 'Atlas Blue',
  graphite: 'Graphite',
  nord: 'Nord',
  cobalt: 'Cobalt',
  harbor: 'Harbor',
  executive: 'Executive',
  iris: 'Iris',
  sage: 'Sage',
  copper: 'Copper',
  glacier: 'Glacier',
};

export type BuiltInPaletteName = keyof typeof palettes;
